## What changed

<!-- One or two sentences. -->

## Verification

- [ ] `mise install` and `mise run check`
- [ ] Installed and tested on Windows (when possible)
- [ ] `python -m unittest discover -s tests -v`
- [ ] `python scripts/gen_readme_packages.py --check`
